	AREA HEX_BCD, CODE, READONLY
ENTRY
	LDR R0, = 0X00000022
	MOV R3, #0X00
	LDRB R1, [R0]
LOOP 	CMP R1, #0X64
	BLT NEXT
	SUB R1, R1, #0X64
	ADD R2, R2, #1
	B LOOP
NEXT 	CMP R1, #0X0A
	BLT LOOP1
	SUB R1, R1, #0X0A
	ADD R3, R3, #1
	B NEXT
LOOP1	ADD R1, R3, LSL#4
	STRB R2, [R0, #1]
	STRB R2, [R0, #2]
STOP	B STOP
	END